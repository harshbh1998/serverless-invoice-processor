# boto3 is pre-installed in all Lambda Python runtimes.
# List it here only so local development environments match Lambda.
boto3>=1.34.0
botocore>=1.34.0

# Testing (not included in Lambda package)
pytest>=8.0.0
